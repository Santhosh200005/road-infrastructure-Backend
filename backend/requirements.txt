# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# NOTE: passlib 1.7.4's bcrypt backend-detection code is incompatible with
# bcrypt>=4.1 (raises "password cannot be longer than 72 bytes" on the
# library's internal self-test, unrelated to actual password length).
# Pinned here until passlib releases a fix.

# --- Config ---
python-dotenv==1.0.1
pydantic[email]==2.7.1

# --- ML dependencies (required by ml/detection and ml/simulator — unchanged from Person A's setup) ---
ultralytics==8.2.18
torch>=2.2.0
torchvision>=0.17.0
xgboost==2.0.3
scikit-learn==1.5.0
pandas==2.2.2
numpy==1.26.4
requests==2.32.3

# --- Agentic AI (CrewAI 4-agent workflow: Priority/Budget/Schedule/Traffic) ---
crewai==0.51.1
langchain-openai==0.1.17

# --- Testing ---
pytest==8.2.2
httpx==0.27.0
